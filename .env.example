# Pi-hole DNS API Configuration

# HTTP server port
PORT=3000

# Path to Pi-hole custom DNS records file
DNS_FILE_PATH=/etc/pihole/custom.list

# Path to backup file (used by /reset endpoint)
DNS_FILE_BACKUP_PATH=/etc/pihole/custom.list.old

# Command to restart Pi-hole DNS resolver
PIHOLE_RESTART_CMD=pihole restartdns

# API key for authentication (leave empty to disable auth)
API_KEY=

# Base URL for the Pi-hole DNS API (used by client library)
PIHOLE_DNS_API_URL=http://172.30.0.110:3000
